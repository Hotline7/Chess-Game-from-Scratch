<Window x:Class="ChessUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="High-Performance Bitboard Chess" Height="680" Width="680"
        WindowStartupLocation="CenterScreen" Background="#1E1E1E">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border BorderBrush="#111" BorderThickness="4" CornerRadius="4" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <UniformGrid x:Name="ChessBoardGrid" Rows="8" Columns="8" Width="560" Height="560"/>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button Content="Flip Perspective" Padding="15,6" Background="#333" Foreground="White" BorderThickness="0" Click="FlipBoard_Click"/>
        </StackPanel>
    </Grid>
</Window>